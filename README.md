# FFT-Mechanics
An implementation of FFT based mechanics solver

# Run
First, put a image named `mat.png` in data folder, the resolution must match the `W` and `H` defined in `main.rs`, then run
```console
cargo run -p exe
```